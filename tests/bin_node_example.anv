Node := struct {
	c : u32;
	d : u32;
};

BinNode := struct {
	a : & Node;
	b : i32;
};

fn test(input : BinNode) -> i8 {
	mut node := move @alloc(Node.{c = 1; d = 2;});

	mut node1 : &.own Node = move node;
	mut node2 : &Node = node;
}
