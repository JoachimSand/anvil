BinNode := struct {
	a : i32;
	b : i32;
};

fn test(input : BinNode) -> i8 {
	recv := input;

	mut left : &.own BinNode = @alloc(BinNode.{ a = 2; b = 3;});
	second := move left;
	res := @free(second);
}
